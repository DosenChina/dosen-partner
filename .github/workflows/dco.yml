name: DCO Check

on:
  pull_request:
    branches: [ master ]

jobs:
  dco:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: DCO Check
      uses: actions/dco@v1
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
